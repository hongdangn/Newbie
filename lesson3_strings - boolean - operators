#slicing string - return a range of characters by using the slice syntax
x = "banana,dang"
print(x[1:4]) #get the chars from 1 to 4(not included)
print(x[:4]) #slice from the start
print(x[4:]) #slice from the end
print(x.split(",")) #split the string to 2 part (the result will be ['banana', 'dang'] - list)

#insert a number into a strings
age = 36
txt = "my name is dang and i am {}"
print(txt.format(age))

#the format() method takes unlimited number of arguments, and are placed into the respective placeholders
quantity = 3
soluong = 567
price = 49.95
myorder = "I want {} pieces of item {} for {} dollars."
print(myorder.format(quantity, soluong, price))

#isinstance() function, which can be used to determine if an object is of a certain data type
x = 200
print(isinstance(x, int))
